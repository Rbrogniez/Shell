#!/usr/bin/env bash

FILE=school

if [ -e "$FILE" ]
then
    echo "$FILE exists."
else 
    echo "$FILE does not exist."
fi

if [ -s "$FILE" ]
then
    echo "$FILE file is empty"
else
    echo "$FILE file is not empty"

    fi
